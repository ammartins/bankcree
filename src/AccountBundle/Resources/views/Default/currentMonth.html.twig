<table class="table table-condensed table-hover table-responsive">
  <tr>
    <th class="text-left">Transaction Date</th>
    <th class="text-right">Saldo</th>
    <th class="text-right">Ammount</th>
    <th class="text-right">Short Description</th>
    <th class="text-right">Options</th>
  </tr>
  {% for transaction in transactions %}
    <tr {% if transaction.amount > 0 %} class='success' {% endif %}>
      <td>{{ transaction.createAt|date("d/m/Y") }}</td>
      <td class="text-right">{{ transaction.endsaldo }} €</td>
      <td class="text-right">{{ transaction.amount }} €</td>
      <td class="text-right">{{ transaction.shortDescription|capitalize }}</td>
      <td class="text-right">
        <a href="{{ path('show', {'id': transaction.id}) }}">Show</a>
        <a href="{{ path('edit', {'id': transaction.id}) }}">Edit</a>
      </td>
    </tr>
  {% endfor %}
</table>

<nav class="text-center">
  <ul class="pagination" id="months">
    {% for month in months %}
      <li {% if month.months == currentMonth%}class="active"{% endif %}>
        <a id="month-{{ month.months }}" href="#{{ month.months }}">
          {{ month.months }}
        </a>
      </li>
    {% endfor %}
  </ul>
</nav>
